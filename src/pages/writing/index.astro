---
import ScrollsView from '@/components/ScrollsView.astro';
// `/` is the canonical landing; `/writing` renders the same view so existing
// links (post "Back to Scrolls", RSS, sitemap) keep working without a redirect.
---
<ScrollsView canonicalPath="/" />
